runtime: java17
# NOTE: This file is NOT used for deployment.
# Deploy with: mvn clean package -DskipTests -P gae-executable
#              gcloud app deploy target/appengine-staging
# The gae-executable profile generates target/appengine-staging/app.yaml with the correct WAR name.
#
# If you must use mvn appengine:deploy, update the WAR name below to match <version> in pom.xml.
entrypoint: java -Dserver.port=$PORT -jar dndapp-15.war
